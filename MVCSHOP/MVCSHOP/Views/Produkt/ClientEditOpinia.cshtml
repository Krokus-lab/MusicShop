@model MVCSHOP.Models.Opinia

@{
    ViewData["Title"] = "ClientEditOpinia";
}

<h1>Edytuj swoją opinię:</h1>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="ClientEditOpinia">    
            <div class="form-group">
                <label asp-for="Tresc" class="control-label"></label>
                <input  asp-for="Tresc" value="@ViewData["TrescOpini"]" class="form-control" />
                <span asp-validation-for="Tresc" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

