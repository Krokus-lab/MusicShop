@model System.Data.DataTable
@{
    ViewData["Title"] = "Index";
}

<h1>Opinie:</h1>

<table class="table table-bordered table-striped">
    <tr>
        <th>Autor opinii:</th>
        <th>Treść:</th>

        <th></th>
    </tr>
    @for (int i = 0; i < Model.Rows.Count; i++)
    {
<tr>
    <td>@Model.Rows[i][2]</td>
    <td>@Model.Rows[i][1]</td>
    <td>
        <a href="@Url.Action("Opinia_edit", "Produkt", new { @id=Model.Rows[i][0] })">Edycja</a>/
        <a href="@Url.Action("Opinia_delete", "Produkt", new { @id=Model.Rows[i][0] })">Usuń</a>
    </td>
</tr>
    }
</table>

<a asp-action="Index">Back to List</a>